{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bosunpkm.com/schemas/v1/realms/32-registry.schema.json",
  "title": "Bosun Realm 32: Registry Contract v1",
  "description": "Machine-readable delta schema contract for Registry (Realm 32), extending Archetype Sovereign Vault and Universal Meta-Envelope with realm-specific payload attributes.",
  "type": "object",
  "version": "1.0.0",
  "allOf": [
    {
      "$ref": "https://bosunpkm.com/schemas/v1/archetypes/sovereign-vault.schema.json"
    },
    {
      "$ref": "https://bosunpkm.com/schemas/v1/meta/envelope.schema.json"
    },
    {
      "type": "object",
      "properties": {
        "$pkm": {
          "type": "object",
          "properties": {
            "realm": {
              "const": "registry",
              "description": "Canonical realm identifier for Registry."
            }
          }
        },
        "document_type": {
          "type": "string",
          "description": "Identity or civil registration document category (passport, birth_certificate, etc.)."
        },
        "issuing_jurisdiction": {
          "type": "string",
          "description": "Issuing sovereign nation, state, or municipal authority."
        },
        "expiry_date": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "Document validity expiration date (YYYY-MM-DD)."
        },
        "encrypted_credential_cas": {
          "type": "string",
          "pattern": "^urn:[a-zA-Z0-9_.:-]+$",
          "description": "CAS content hash of the encrypted credential payload."
        }
      }
    }
  ],
  "additionalProperties": true
}
