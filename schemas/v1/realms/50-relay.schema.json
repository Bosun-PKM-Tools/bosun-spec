{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bosunpkm.com/schemas/v1/realms/50-relay.schema.json",
  "title": "Bosun Realm 50: Relay Contract v1",
  "description": "Machine-readable delta schema contract for Relay (Realm 50), extending Archetype Sovereign Vault and Universal Meta-Envelope with realm-specific payload attributes.",
  "type": "object",
  "version": "1.0.0",
  "allOf": [
    {
      "$ref": "https://bosunpkm.com/schemas/v1/archetypes/sovereign-vault.schema.json"
    },
    {
      "$ref": "https://bosunpkm.com/schemas/v1/meta/envelope.schema.json"
    },
    {
      "type": "object",
      "properties": {
        "$pkm": {
          "type": "object",
          "properties": {
            "realm": {
              "const": "relay",
              "description": "Canonical realm identifier for Relay."
            }
          }
        },
        "dead_man_interval_days": {
          "type": "integer",
          "minimum": 1,
          "description": "Check-in cadence before automated trigger execution."
        },
        "heartbeat_received_at": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}(T[0-9]{2}:[0-9]{2}(:[0-9]{2}(\\.[0-9]+)?)?(Z|[+-][0-9]{2}:?[0-9]{2})?)?$",
          "description": "ISO-8601 timestamp of last confirmed heartbeat."
        },
        "master_recovery_key_cas": {
          "type": "string",
          "pattern": "^urn:[a-zA-Z0-9_.:-]+$",
          "description": "CAS hash of the split-secret master recovery payload."
        }
      }
    }
  ],
  "additionalProperties": true
}
