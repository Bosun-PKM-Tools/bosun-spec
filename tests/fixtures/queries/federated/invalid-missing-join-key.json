{
  "plan_id": "018f3a00-0000-7000-8000-0000000000f3",
  "title": "Invalid Primary↔Archive join with missing join keys",
  "description": "Two-vault sovereign bridge that declares a join step but omits every join-key definition, so identity correlation cannot be validated.",
  "version": "1.0.0",
  "vault_endpoints": {
    "primary": {
      "type": "local",
      "path": "fixtures/synthetic_vault",
      "read_only": true,
      "format": "markdown_pkm"
    },
    "archive": {
      "type": "stdio_rpc",
      "command": "harbormaster",
      "args": ["engine", "stdio", "--vault", "/vaults/archive"],
      "protocol": "jsonrpc-2.0"
    }
  },
  "distributed_steps": [
    {
      "step_id": "scan_primary_live_notes",
      "target_vault": "primary",
      "operation": "traverse",
      "query": {
        "linked_from": "urn:trice:task:018f3a00-0000-7000-8000-000000000003",
        "match_realm": ["trice", "yeoman"],
        "projections": ["nodes"]
      }
    },
    {
      "step_id": "join_primary_archive_urns",
      "target_vault": "archive",
      "operation": "join",
      "input_from_step": "scan_primary_live_notes",
      "join_with_step": "scan_primary_live_notes",
      "join_key": "task_derived_from_project",
      "join_type": "inner"
    }
  ],
  "join_keys": {}
}
